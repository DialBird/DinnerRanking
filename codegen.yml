# HasuraのURLに対してinspectした結果
schema: schema.graphql
documents: ./src/**/*.{graphql,gql}
generates:
  ./src/generated/graphqlOperations.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
